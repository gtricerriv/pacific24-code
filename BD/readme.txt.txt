After cloning, rename the parent folder to pacific54

*Requirements:
 -xampp7 (7.4.30 / PHP 7.4.30	).

 -Modify the file php.ini (C:\xampp7\php)
	
	max_execution_time=120
	upload_max_filesize=256M
	post_max_size=140
	memory_limit=512M
	post_max_size=512M

 -Decompress Wordpress on: C:\xampp7\htdocs

 -Import database and edit: wp-config.php

 -Access data
: pacific54 (user), Uzfs3xt4 (clave)

 -In the dashboard de Wordpress  Elementor > Tools > Regenerate CSS requisitos& Data y Sync Library	

 -In the dashboard de Wordpress ir a Settings > Permanent link > Save (without making changes)

Note: The folder has to be called pacific54 because that is how the routes are defined in the database.
	

